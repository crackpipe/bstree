# bstree

it's a binary search tree written in C.

## functions

- Insertion (`bst_insert`)
- Search (`bst_search`)
- Deletion (`bst_delete`)
- Sorting (`treesort_i` and `treesort_d`)

## how to use

read `main.c`
